import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#ff8100',
        },
        secondary: {
            main: '#5fe0b7',
        },
    },
});

export default theme;